# Recommendation algorithm

The first ranking contract combines semantic similarity, keyword overlap, citation relationships, topic affinity, and recency. The initial UI uses deterministic development records and exposes the explanation fields needed by a production ranker.

The AI service will later provide Sentence Transformer embeddings and cosine similarity. Ranking remains in the backend recommendation service.
